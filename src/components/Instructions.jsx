export default function Instructions() {
  return (
    <div>
      <div className="instructions">
        <p>
          Step 1: Either paste URL of JSON into the input OR paste JSON directly
          into textbox
        </p>
        <p>Step 2: Click either Extract URL or Extract JSON</p>
        <p>
          Step 3: Any syntax errors with the JSON will be automatically
          corrected
        </p>
        <p>Step 4: You will also have an outline showing all the nexted Keys</p>
        <p>Step 5: You can hover on the keys to see their values</p>
        <p>Step 6: You can click on the keys to get the full path</p>
      </div>
    </div>
  );
}
